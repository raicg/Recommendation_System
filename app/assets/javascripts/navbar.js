//this script is used only on mobile navigation to show a menu with the options on navbar

$("a.navbar-burger").click(function() {

    // Toggle the "is-active" class on the "navbar-menu"
    $("div.navbar-menu").toggleClass("is-active");

});
